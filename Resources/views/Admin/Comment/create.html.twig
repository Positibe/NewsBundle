<h1>Comment creation</h1>

<form action="{{ path('positibe_news_admin_news_comment_create', {'slug': comment.news.slug}) }}" method="post" {{
form_enctype(form)
}}>
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn btn-default">Submit</button>
</form>
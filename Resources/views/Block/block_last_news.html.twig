{% for content in contents %}
    {% set page = content %}
    <a href="{{ path(page) }}" class="content-title">{{ page.title|raw }}</a>
    <p class="content-body">{{ page.body | striptags | decode_html | truncate(150) }}</p>
    <hr>
{% endfor %}